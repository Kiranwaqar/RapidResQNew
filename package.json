{
  "name": "rapid-res-qn-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "vercel-build": "npm run build && mkdir -p .vercel/output/public && cp -r frontend/build/* .vercel/output/public/"
  }
}
